import { ExtractExtraFromPollOptionPipe } from './extract-extra.pipe';

describe('ExtractExtraPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractExtraFromPollOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
